#include "main.h"

/**
 * _abs - returns absolute value of int
 * @int: value returned
 *
 * Return: 0 if success.
 */

int _abs(int)
{
	if (int < 0)
	{
		int = int * -1;
		return (int);
	}
	return (int);
}
